import { DataPoint } from './types'

const dataPoints: DataPoint[] = [
  {
    description: 'Mock datapoint',
    coordinates: [10, 10],
    id: 1,
    severity: 1,
    radius: 10
  }
]

export default dataPoints
